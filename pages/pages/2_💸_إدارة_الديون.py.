import streamlit as st

st.title("💸 مراقبة الديون والتحصيل")

st.info("هذا القسم يراقب المبالغ المعلقة لدى العملاء وتنبيهات مواعيد التحصيل.")

# مثال تفاعلي
customer_name = st.text_input("اسم العميل")
debt_amount = st.number_input("المبلغ المتبقي (ج.م)", value=0)

if st.button("تسجيل مديونية"):
    st.warning(f"تم تسجيل {debt_amount} جنيه على {customer_name}. سيتم إرسال تنبيه آلي عند موعد السداد.")
